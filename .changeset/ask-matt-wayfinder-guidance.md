---
"mattpocock-skills": patch
---

Make `$mattpocock-skills:ask-matt` clued-up about `$mattpocock-skills:wayfinder` — the heaviest, most cognitively demanding flow.

The router now sharpens the two routing mistakes people most often make with wayfinder:

- **Over-reaching for it.** It's slower and denser than a single grill, so it's flagged as the heaviest flow and reserved for the idea that genuinely won't fit one session — a well-scoped feature belongs on `$mattpocock-skills:grill-with-docs`, not here.
- **Losing the way at the handoff.** When the map clears, wayfinder hands off, it doesn't build: merge onto the main flow at `$mattpocock-skills:to-spec` (which collapses the map's linked decisions into a buildable plan) rather than looping the map straight into `$mattpocock-skills:implement`. Straight-to-`$mattpocock-skills:implement` is only for efforts that turned out genuinely small.
