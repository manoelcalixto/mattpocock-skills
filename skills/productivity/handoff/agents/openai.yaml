interface:
  display_name: "Handoff"
  short_description: "Compact a conversation into a handoff"
  default_prompt: "Use $handoff to choose the right Codex session transition."
policy:
  allow_implicit_invocation: false
