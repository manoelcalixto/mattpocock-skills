interface:
  display_name: "Implement"
  short_description: "Build work from a spec or tickets"
  default_prompt: "Use $implement to build this spec or ticket set with tests and final review."
policy:
  allow_implicit_invocation: false
