interface:
  display_name: "To Spec"
  short_description: "Turn a conversation into a spec"
  default_prompt: "Use $to-spec to synthesize this conversation into a spec without another interview."
policy:
  allow_implicit_invocation: false
