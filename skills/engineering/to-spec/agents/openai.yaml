interface:
  display_name: "To Spec"
  short_description: "Turn a conversation into a spec"
  default_prompt: "Use $mattpocock-skills:to-spec to synthesize this conversation into a spec."
policy:
  allow_implicit_invocation: false
